#include "pcommon/q_para.h"
#include <string>

void SV_PARA_INIT();

int pjkCvarIntValue(char const * name);
float pjkCvarFloatValue(char const * name);
std::string pjkCvarStrValue(char const * name);
